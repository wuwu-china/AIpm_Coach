# AIPMCoach 是 BYOK（Bring Your Own Key）架构：
# 应用本身不内置任何大模型 Key，也不产生 token 费用。
# 使用者在页面右上角「AI 设置」里填入自己的 Key，Key 只保存在其本人浏览器 localStorage。
#
# 本项目的中转函数 /api/chat 是无状态的：它只把请求转发给大模型厂商，
# 不读取任何服务端环境变量里的 Key，因此部署到 Vercel 时【不需要】配置任何 Secret。
#
# 如果你把架构改成「服务端统一出资调用」，才需要在 Vercel 环境变量里配置下面这类变量
# （当前开源版本并不会用到，仅作示例）：
# OPENAI_API_KEY=sk-xxxx
